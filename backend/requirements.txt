# ── Core Framework & Web Server ──
fastapi
uvicorn[standard]
python-multipart

# ── Validazione & Configurazione ──
pydantic[email]
pydantic-settings
email-validator

# ── Autenticazione & Sicurezza ──
python-jose[cryptography]
passlib[bcrypt]
bcrypt
argon2-cffi

# ── Database & Client HTTP ──
aiosqlite
httpx

# ── AI Ecosystem (Gemini & Orchestration) ──
crewai[google-genai]>=0.30.0
crewai-tools
litellm
google-generativeai
google-genai

# ── Web Search — Cascata Google → Tavily → DuckDuckGo ──
tavily-python
duckduckgo-search

# ── SPRINT 4 — Async infrastructure ──────────────────────────────────────────
celery[redis]>=5.3.0          # task queue + Redis broker/backend
redis>=5.0.0                  # client Redis (job_store.py)
# flower>=2.0.0               # opzionale: dashboard monitoraggio Celery
#                             # avvio: celery -A app.worker flower --port=5555

# ── Integrazioni & Utility ──
stripe
python-docx
python-dotenv